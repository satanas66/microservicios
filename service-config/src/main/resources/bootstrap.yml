
server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/satanas66/microservicios.git'
          searchPaths: config-data
          username: satanas66
          password: ODFenergia37GIT
  security:
    user:
      name: root
      password: s3cr3t
